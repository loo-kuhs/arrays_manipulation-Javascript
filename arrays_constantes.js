const beers = ["erdinger", "corona", "delirium"];

/**
 * Arrays constantes (const).
 *    ! No permite la resignación de variables globalmente.
 *    ! e.g. Usar beers = []; después de haberlo ya asignado a un valor.
 *    ! Daría un error
 *
 *    * Pero si podemos mutar sus elementos singulares.
 *    * e.g. beers.push("budweiser"); para agregar un elemento.
 */
